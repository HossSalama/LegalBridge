@inherits LayoutComponentBase
@inject IJSRuntime JSRuntime

<div class="min-h-screen bg-slate-100" dir="rtl">
    <aside class="fixed right-0 top-0 h-screen w-64 bg-slate-900 text-white hidden lg:block border-l border-slate-800 shadow-2xl">
        <NavMenu />
    </aside>

    <div class="lg:mr-64">
        <header class="bg-white border-b border-slate-200 sticky top-0 z-10 shadow-sm">
            <div class="flex items-center justify-between px-4 lg:px-6 h-16">

                <div class="flex items-center gap-2">
                    <h2 class="font-semibold text-slate-900">„—Õ»«° √” «– √Ì„‰</h2>
                </div>

                <div class="flex items-center gap-4">
                    <i data-lucide="bell" class="h-5 w-5 text-slate-500 cursor-pointer"></i>
                    <div class="h-8 w-8 rounded-full bg-amber-600 flex items-center justify-center text-white text-sm">
                        √
                    </div>
                </div>
            </div>
        </header>

        <main class="p-4 lg:p-6">
            @Body
        </main>
    </div>
</div>

@code {
    protected override async Task OnAfterRenderAsync(bool firstRender)
    {
        await JSRuntime.InvokeVoidAsync("initializeIcons");
    }
}